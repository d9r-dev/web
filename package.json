{
  "scripts": {
    "start:parcel": "npx parcel index.html",
    "start:db": "json-server --watch db.json"
  },
  "devDependencies": {
    "json-server": "^0.17.3",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "axios": "^1.5.0"
  }
}
